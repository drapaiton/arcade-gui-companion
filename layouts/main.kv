<RootMenu>
    canvas:
        Color:
            rgb: 1,0,0
        Rectangle:
            size: self.size
            pos: self.pos
    PrincipalButtons:


<PrincipalButtons>
    padding: 50
    spacing:80
    orientation: 'horizontal'

    BoxLayout:
        orientation: 'vertical'
        spacing:75
        Button:
            on_press:
                print(dir(self))
        Button:

    BoxLayout:
        orientation: 'vertical'
        spacing:75
        Button:
        Button:
    BoxLayout:
        orientation: 'vertical'
        spacing:75
        Button:
        Button:
    BoxLayout:
        orientation: 'vertical'
        spacing:75
        Button:
        Button:
    BoxLayout:
        orientation: 'vertical'
        spacing:75
        Button:
        Button:
    BoxLayout:
        orientation: 'vertical'
        spacing:75
        Button:
        Button: